import Math, { sum, minus, multiply, divide, summation, pow } from './lib.js';
import { sqrt } from 'mathjs';

console.log(Math.sum(2, 1)); // 3
console.log(sum(2, 1)); // 3
console.log(minus(2, 1)); // 1
console.log(multiply(2, 1)); // 2
console.log(divide(2, 1)); // 2
console.log(summation(1, 2, 3, 4)); // 10
console.log(pow(2, 3)); // 8
console.log(sqrt(16)); // 4
